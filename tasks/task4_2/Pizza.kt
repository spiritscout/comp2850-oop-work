

fun main() {
    println("Select an option: a b c d ")
    val option = readln().lowercase()
    val choice = if (val option = readln().lowercase()) {
        println("Order Accepted")
    } else {
        println("Invalid Choice")
    }
}